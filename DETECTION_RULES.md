# EcoWarden 객체 구별 규칙 상세 (Detection Rules)

> rplidar_app이 **배경**, **사람**, **쓰레기(투기물)** 를 어떻게 구별하는지에 대한 상세 문서
> 2026-07-04 포지셔닝 전환 후 이 문서는 **보안 감시 시스템의 LiDAR 탐지 엔진 명세**로 사용된다 — 여기서 확정된 "사람" 트랙이 금지구역 침입 판정(`include/zone_policy.h`, README §3.7)의 입력이 되고, 투기 구별 규칙은 확장 기능으로 유지된다.

---

## 1. 전체 판별 흐름 요약

```
LiDAR 원시 데이터 (극좌표, 10Hz)
  │
  ▼
[1단계] 노이즈 필터 — 거리, 강도, 각도로 무효 데이터 제거
  │
  ▼
[2단계] 정적 배경 필터 — 벽/바닥 등 고정 구조물 제거
  │
  ▼
[3단계] DBSCAN 클러스터링 — 남은 점들을 물체 단위로 묶음
  │
  ▼
[4단계] 클러스터 크기 필터 + 다리 병합
  │
  ▼
[5단계] 배경 물체 필터 — 가구 등 이동하지 않는 물체 제거
  │
  ▼
[6단계] 트래커 — 프레임 간 물체 추적 (사람 vs 정지 물체 분류)
  │
  ▼
[7단계] 투기 감지 — 사람에게서 분리된 정지 물체 = 쓰레기 판정
```

---

## 2. 1단계: 노이즈 필터 (유효 데이터 선별)

LiDAR에서 들어오는 원시 데이터 중 물리적으로 의미 없는 데이터를 제거합니다.

### 필터 조건

| 조건 | 값 | 의미 |
|------|-----|------|
| **최소 거리** | 150mm | 15cm 미만은 센서 자체 반사 → 제거 |
| **최대 거리** | 12,000mm | 12m 초과는 RPLiDAR S2 측정 한계 → 제거 |
| **최소 반사 강도** | 10 | 반사가 너무 약한 점은 노이즈 → 제거 |
| **시야각 (FOV)** | 0° ~ 180° | 전방 반원만 사용 (후방은 벽/설치면) |

### 각도 설명

```
         90°
          │
          │ ← 감지 영역 (0°~180°)
          │
180° ─────●───── 0° ● = LiDAR 센서 위치
          │
          │ ← 비감지 영역 (180°~360°)
          │
         270°
```

- LiDAR는 360도 회전하지만, **전방 반원(0°~180°)만 사용**
- 센서를 벽면에 설치하므로 후방(180°~360°)은 벽 뒤 → 불필요

---

## 3. 2단계: 정적 배경 필터 (벽/바닥 제거)

### 동작 원리

- 360도를 **0.1도 단위 3,600개 슬롯**으로 나눔
- 각 슬롯에 해당 각도의 **벽까지 거리**를 EMA(지수이동평균)로 학습
- EMA 비율: 새 값 10%, 기존 값 90% → 안정적 학습
- 학습된 벽 거리보다 **100mm 이상 가까운 점만 통과** (벽에 붙은 점은 제거)

### 판정 기준

```
벽 거리 = 2000mm (학습된 값)
마진 = 100mm

→ 1900mm 이하인 점만 통과 (벽보다 100mm 이상 튀어나온 물체)
→ 1901mm~2100mm 범위의 점 = 벽면으로 판정 → 제거
```

### 순서 (v7 수정)

```
1. Process() — 이전 프레임에서 학습한 배경 기준으로 필터
2. LearnStaticBackground() — 이번 프레임으로 배경 학습 (다음 프레임부터 적용)
```

첫 프레임에서는 배경 데이터가 없으므로(모든 슬롯 = 0) **모든 점이 통과**합니다.

---

## 4. 3단계: DBSCAN 클러스터링 (점 → 물체 묶기)

남은 점들을 밀도 기반으로 묶어서 개별 물체(클러스터)로 만듭니다.

### 파라미터

| 파라미터 | 값 | 의미 |
|----------|-----|------|
| **ε (epsilon)** | 100mm | 이웃 탐색 반경 — 10cm 이내 점은 같은 물체 |
| **min_points** | 5 | 최소 5개 점이 모여야 물체로 인정 |

### 동작

1. 각 점에서 100mm 반경 내 이웃 점 수를 셈
2. 이웃이 5개 이상이면 **코어 포인트** (물체의 중심부)
3. 코어 포인트끼리 연결되면 하나의 클러스터로 합침
4. 이웃이 5개 미만이면 **NOISE** → 제거

### 가속

- Grid-based 공간 인덱스 사용 (셀 크기 = ε × ε)
- 평균 O(n) 시간 복잡도 (매 프레임 1~3ms)

---

## 5. 4단계: 클러스터 크기 필터 + 다리 병합

### 크기 필터

| 조건 | 값 | 의미 |
|------|-----|------|
| **최소 폭** | 50mm | 5cm 미만 = 노이즈/먼지 → 제거 |
| **최대 폭** | 1,200mm | 1.2m 초과 = 벽/대형 구조물 → 제거 |

### 다리 병합 (사람 인식 핵심)

사람은 두 다리가 별도 클러스터로 잡힐 수 있습니다. 이를 하나로 합칩니다.

| 조건 | 값 | 의미 | |
| ----------- | ------- | ----------------------------------- | --- |
| **병합 반경** | 350mm | 두 클러스터 중심 간 35cm 이내면 병합 후보 | |
| **폭 비율 제한** | 2.5배 이하 | 비슷한 크기의 쌍만 병합 (다리끼리는 OK, 사람+봉투는 NO) | |

```
예시:
  다리A (폭 80mm) ──── 300mm ──── 다리B (폭 90mm)
  → 중심 거리 300mm < 350mm
  → 폭 비율 90/80 = 1.13 < 2.5
  → 하나의 "사람" 클러스터로 병합
```

---

## 6. 5단계: 배경 물체 필터 (가구 등 제거)

### 학습 단계 (시작 후 50프레임 = 5초)

- 매 프레임의 클러스터 위치를 누적
- 5초 동안 **계속 같은 위치에 있는 클러스터 = 배경 물체**(가구, 쓰레기통 등)로 등록
- 등록 기준: 30% 이상의 프레임에서 관측된 위치

### 운용 단계 (학습 완료 후)

- 배경 맵에 등록된 위치와 **80mm 이내**인 클러스터 = 배경 → 제거
- 남은 클러스터 = **전경 물체** (사람, 새로 놓인 물건 등)

### 적응형 배경 갱신 (장기 운영)

| 동작 | 조건 | 의미 |
|------|------|------|
| 단기 배경 등록 | 300프레임(30초) 연속 관측 | 새로 놓인 가구 → 배경으로 등록 |
| 단기 배경 제거 | 500프레임(50초) 연속 미관측 | 치워진 가구 → 배경에서 제거 |
| 장기 배경 등록 | 1,000프레임(100초) 연속 관측 | 확실한 고정 물체 |
| 장기 배경 제거 | 3,000프레임(5분) 연속 미관측 | 장기 부재 |

### 투기물 보호

투기로 확정된 물체의 위치는 `SetDumpedPositions()`로 배경 필터에 피드백 → **투기물이 배경으로 등록되는 것을 방지**

---

## 7. 6단계: 트래커 — 사람 vs 정지 물체 분류

### 클러스터-트랙 매칭

매 프레임 새 클러스터를 기존 트랙에 매칭합니다.

| 파라미터 | 값 | 의미 |
|----------|-----|------|
| **매칭 최대 거리** | 400mm | 이전 위치에서 40cm 이내면 같은 물체 |
| **KF 예측 fallback** | 300mm | 1차 매칭 실패 시 칼만 필터 예측 위치 기준 재매칭 |
| **적응형 게이트** | 3σ | KF 불확실성에 비례해 게이트 자동 확장 |

### 트랙 상태 머신 (4단계)

```
┌──────────┐ 이동거리 ≥ 30mm ┌──────────┐
│ MOVING │◄────────────────────│STATIONARY│
└────┬─────┘ └────┬─────┘
     │ 이동거리 < 30mm │ 정지 20프레임 연속 (2초)
     ▼ ▼
┌──────────┐ ┌──────────┐
│STATIONARY│ │ DEPARTED │ ← 이탈 이벤트 1회 발화
└──────────┘ └──────────┘

LOST: 매칭 실패 15프레임(1.5초) → 트랙 삭제
```

### 상태 판정 기준

| 판정 | 조건 | 시간 |
|------|------|------|
| **이동 중 (사람)** | 프레임 간 이동거리 ≥ 30mm | 즉시 |
| **정지** | 프레임 간 이동거리 < 30mm | 즉시 |
| **이탈** | 연속 정지 20프레임 | 2초 (10Hz 기준) |
| **소실** | 매칭 실패 15프레임 | 1.5초 → 트랙 삭제 |

### 칼만 필터 (궤적 예측)

- **상태 벡터**: `[x, y, vx, vy]` (위치 + 속도)
- **관측**: `[x, y]` (클러스터 중심)
- **프로세스 노이즈**: 50 (모델 불확실성)
- **관측 노이즈**: 100 (센서 불확실성)
- 수치 안정: Joseph form 공분산 업데이트

---

## 8. 7단계: 투기 감지 — 쓰레기 판별 전 과정

### 8.1 투기 감지의 핵심 원리

```
사람(이동 클러스터)이 걷다가
  → 몸에서 물체가 분리됨 (새 정지 클러스터 출현)
  → 사람은 계속 이동하여 멀어짐
  → 물체는 그 자리에 남아있음
  = 쓰레기 투기
```

### 8.2 투기 주체(사람) 자격 조건

투기를 할 수 있는 "사람"으로 인정되려면:

| 조건 | 값 | 의미 |
|------|-----|------|
| **최소 누적 이동거리** | 500mm | 50cm 이상 걸은 이력 필요 (정지 물체가 사람으로 오인되는 것 방지) |
| **최소 생존 프레임** | 10프레임 (1초) | 너무 짧게 보인 클러스터는 제외 |

### 8.3 투기물(쓰레기) 후보 조건

새로 나타난 클러스터가 투기물 후보가 되려면:

| 조건 | 값 | 의미 |
|------|-----|------|
| **최소 폭** | 10mm | 1cm 이상 (작은 병 감지 가능) |
| **최대 폭** | 800mm | 80cm 초과는 사람/가구 → 투기물 아님 |
| **최소 점구름 수** | 2개 | 점이 1개뿐이면 노이즈 |

### 8.4 분리 감지 (4가지 경로)

사람에게서 물체가 떨어져 나왔다고 판단하는 4가지 방법:

#### Path 1: 궤적 이력 매칭 (주 경로)

```
사람 궤적: A → B → C → D → E (현재)
새 클러스터 출현 위치: C 근처

→ 사람의 과거 위치(궤적 이력 30프레임) 중 하나와 600mm 이내
→ 그리고 사람의 현재 위치에서 200mm 이상 떨어져 있음
→ 분리 감지!
```

| 파라미터 | 값 | 의미 |
|----------|-----|------|
| 궤적 매칭 거리 | 600mm | 사람의 과거 위치에서 60cm 이내 |
| 현재 위치 최소 거리 | 200mm | 현재 위치에서 20cm 이상 (다리 오인 방지) |
| 궤적 이력 보존 | 30프레임 (3초) | 최근 3초간의 위치 기록 |

#### Path 2: 폭 감소 감지 (보조 신호)

```
사람 클러스터 폭: 400mm → 320mm (1프레임에 80mm 이상 감소)
→ 무언가를 놓았을 가능성
→ 감소 발생 위치 근처에 새 클러스터 출현 시 분리 확정
```

| 파라미터 | 값 | 의미 |
|----------|-----|------|
| 폭 감소 임계값 | 80mm | 1프레임에 8cm 이상 줄면 감지 |

#### Path 3: 클러스터 분열 감지 (보조 신호)

```
사람 클러스터 근처에 미매칭 클러스터 출현
→ 사람 폭 + 200mm 반경 내 (최대 500mm 제한)
→ 분열 발생으로 판단
```

| 파라미터 | 값 | 의미 |
|----------|-----|------|
| 분열 감지 여유 반경 | 사람 폭 + 200mm | 사람 크기에 비례 |
| 반경 상한 | 500mm | 너무 넓은 감지 방지 (오탐 방지) |

#### Path 4: 정지 트랙 궤적 재매칭 (Phase 6.5)

이미 정지 상태인 트랙이 사람의 궤적 위에 있는 경우:

| 조건 | 값 |
|------|-----|
| 정지 프레임 ≥ 5 | 0.5초 이상 정지 |
| 누적 이동거리 ≤ 200mm | 거의 움직이지 않은 물체 |
| 보조 신호 1개 이상 필요 | 폭 감소 / 분열 / 10프레임 이상 정지 중 하나 |

### 8.5 의심(Suspect) 확정 과정

분리가 감지된 클러스터는 바로 쓰레기가 아니라 **"의심(suspect)"** 상태로 시작합니다.

#### 의심 해제 조건 (오탐 방지)

| # | 조건 | 의미 |
| --- | ---------------------------------- | --------------------- |
| 1 | 350mm 이내에 다른 사람 트랙 존재 | 사람의 다리로 판단 → 의심 취소 |
| 2 | 의심 기간 중 폭 분산 > 2,500mm² (σ > 50mm) | 크기가 불안정 = 사람의 일부 → 취소 |
| 3 | 10프레임 이상 lost (매칭 실패) | 일시적 노이즈였음 → 취소 |

#### 의심 → 투기물 승격 조건 (전부 충족 필요)

| # | 조건 | 값 | 의미 |
| --- | ------------------ | -------------------------- | --------------------- |
| 1 | 독립 존재 프레임 | ≥ 3프레임 (0.3초) | 일시적 노이즈가 아닌 실체 |
| 2 | 물체 정지 확인 | 속도 < 5mm/frame | 움직이지 않는 물체 |
| 3 | 사람 이탈 확인 | 사람이 200mm 이상 떨어짐 + 멀어지는 방향 | 사람이 실제로 떠나고 있음 |
| 4 | (사람 소실 시) 분리 시점 거리 | ≥ 600mm | 사람이 이미 떠났으면 원래 위치와 비교 |

### 8.6 투기 최종 확정 (Phase 7)

의심 → 투기물로 승격된 후, 최종 확정에 필요한 추가 조건:

| # | 조건 | 값 | 의미 |
|---|------|-----|------|
| 1 | 물체 정지 프레임 | ≥ 10프레임 (1초) | 1초 이상 제자리에 있음 |
| 2 | 사람과의 거리 | ≥ 600mm | 사람이 투기물에서 60cm 이상 떨어짐 |
| 3 | 사람 이동 방향 | 투기물에서 멀어지는 방향 (receding vector > 0) | 다시 돌아오는 중이 아님 |

```
확정 시:
  → [ALERT] 콘솔 출력
  → 카메라 사진 촬영 (base64)
  → FastAPI POST 전송 (증거)
  → Unity UDP 전송 (시각화)
  → Hotspot 등록 (같은 위치 재감지 방지)
```

### 8.7 Hotspot (투기 지점 우선 감지)

과거에 투기가 확정된 위치를 기억합니다.

| 파라미터 | 값 | 의미 |
|----------|-----|------|
| Hotspot 반경 | 500mm | 과거 투기 위치 50cm 반경 |
| Hotspot 수명 | 3,000프레임 (~5분) | 5분 후 자동 만료 |
| Boost 감소 프레임 | 5프레임 | Hotspot 내 의심 객체는 확인 기간 단축 |
| Boost 최소 프레임 | 2프레임 | 최소 0.2초는 확인 (너무 빠른 확정 방지) |
| 최대 Hotspot 수 | 20개 | 메모리 제한 |

### 8.8 추가 오탐 방지 메커니즘

| # | 메커니즘 | 조건 | 의미 |
|---|----------|------|------|
| 1 | Lost track recovery | 600mm 이내 + lost 3프레임 이내 | 잠시 사라졌다 다시 나타난 물체 = 새 투기물 아님 |
| 2 | Deleted position buffer | 삭제된 트랙 위치 30프레임 보존 | 방금 삭제된 트랙 위치에 다시 나타나면 복구 |
| 3 | Active dump block | 확정 투기물 300mm 반경 | 이미 확정된 투기물 근처 재감지 차단 |
| 4 | Person proximity hold | 사람 150mm 반경 | 사람 바로 옆 클러스터는 새 트랙 생성 보류 |
| 5 | 다리 오인 방지 | 450mm 반경 + 폭 300mm 이하 | 사람 근처 작은 클러스터 = 다리로 판단 |

---

## 9. 전체 파라미터 정리표

### 9.1 센서 필터

| 파라미터 | 값 | 단위 |
|----------|-----|------|
| 최소 거리 | 150 | mm |
| 최대 거리 | 12,000 | mm |
| 최소 반사 강도 | 10 | - |
| FOV 최소 | 0 | 도 |
| FOV 최대 | 180 | 도 |
| 정적 배경 마진 | 100 | mm |
| 배경 슬롯 수 | 3,600 | (0.1도 간격) |
| EMA 비율 | 90:10 | 기존:새값 |

### 9.2 클러스터링

| 파라미터 | 값 | 단위 |
|----------|-----|------|
| DBSCAN ε | 100 | mm |
| DBSCAN min_points | 5 | 개 |
| 클러스터 최소 폭 | 50 | mm |
| 클러스터 최대 폭 | 1,200 | mm |
| 다리 병합 반경 | 350 | mm |
| 다리 병합 폭 비율 제한 | 2.5 | 배 |

### 9.3 배경 필터

| 파라미터 | 값 | 단위 |
|----------|-----|------|
| 학습 프레임 수 | 50 | 프레임 (5초) |
| 배경 매칭 반경 | 80 | mm |
| 단기 배경 등록 | 300 | 프레임 (30초) |
| 단기 배경 제거 | 500 | 프레임 (50초) |
| 장기 배경 등록 | 1,000 | 프레임 (100초) |
| 장기 배경 제거 | 3,000 | 프레임 (5분) |

### 9.4 트래커

| 파라미터 | 값 | 단위 |
|----------|-----|------|
| 정지 판단 이동거리 | 30 | mm |
| 이탈 판정 정지 프레임 | 20 | 프레임 (2초) |
| 매칭 최대 거리 | 400 | mm |
| Lost 삭제 프레임 | 15 | 프레임 (1.5초) |
| KF 프로세스 노이즈 | 50 | - |
| KF 관측 노이즈 | 100 | - |
| KF fallback 거리 | 300 | mm |

### 9.5 투기 감지

| 파라미터 | 값 | 단위 |
|----------|-----|------|
| 최소 누적 이동거리 | 500 | mm |
| 최소 생존 프레임 | 10 | 프레임 (1초) |
| 투기물 정지 확인 | 10 | 프레임 (1초) |
| 궤적 매칭 거리 | 600 | mm |
| 현재 위치 최소 거리 | 200 | mm |
| 분리 확인 프레임 | 3 | 프레임 (0.3초) |
| 다리 근접 판정 | 350 | mm |
| 궤적 이력 보존 | 30 | 프레임 (3초) |
| 폭 감소 임계값 | 80 | mm |
| 투기물 최소 폭 | 10 | mm |
| 투기물 최대 폭 | 800 | mm |
| 투기물 최소 점구름 | 2 | 개 |
| 사람 이탈 거리 | 600 | mm |
| 폭 분산 취소 임계값 | 2,500 | mm² (σ>50mm) |

### 9.6 Hotspot / 오탐 방지

| 파라미터 | 값 | 단위 |
|----------|-----|------|
| Hotspot 반경 | 500 | mm |
| Hotspot 수명 | 3,000 | 프레임 (~5분) |
| Hotspot boost | 5 | 프레임 |
| Hotspot boost 최소 | 2 | 프레임 |
| 최대 Hotspot 수 | 20 | 개 |
| Active dump block | 300 | mm |
| Person proximity hold | 150 | mm |
| Lost track recovery | 600 | mm (3프레임 이내) |
| 다리 오인 반경 | 450 | mm |
| 다리 오인 최대 폭 | 300 | mm |

---

## 10. 전송 데이터 형식 (JSON 규격)

### 10.1 Unity 전송 (UDP JSON, tracked_only 모드)

**전송 주기**: 매 프레임 (10Hz = 초당 10회)
**프로토콜**: UDP
**대상**: Unity (`192.168.20.102:5005`)
**모드**: tracked_only — **추적 확인된 객체만** 전송 (고스트/노이즈 제거)

```json
{
    "type": "FRAME",
    "frame_id": 42,
    "timestamp": 1712290800000,
    "objects": [
        {
            "id": 1,
            "x": 1200.0,
            "y": -350.0,
            "type": "normal",
            "width": 380.0,
            "is_abandoned": false
        },
        {
            "id": 2,
            "x": 500.0,
            "y": 800.0,
            "type": "dumped",
            "width": 120.0,
            "is_abandoned": true
        }
    ],
    "tracks": [
        {
            "id": 1,
            "x": 1200.5,
            "y": -350.2,
            "state": "moving",
            "is_dumped_item": false,
            "is_dump_suspect": false
        },
        {
            "id": 2,
            "x": 500.0,
            "y": 800.0,
            "state": "stationary",
            "is_dumped_item": true,
            "is_dump_suspect": false
        }
    ],
    "points": [],
    "events": [
        {
            "type": "dumping",
            "person_track_id": 1,
            "person_x": 1200.0,
            "person_y": -800.0,
            "object_track_id": 2,
            "object_x": 500.0,
            "object_y": 800.0,
            "timestamp": 1712290800000
        }
    ]
}
```

#### 필드 설명

| 필드 | 타입 | 설명 |
|------|------|------|
| `type` | string | 항상 `"FRAME"` |
| `frame_id` | int | 프레임 번호 (1부터 증가) |
| `timestamp` | int | Unix timestamp (밀리초) |
| `objects` | array | **추적 확인된 객체만** (트랙 기반, 고스트 없음) |
| `tracks` | array | 추적 중인 트랙 목록 (Lost 상태 제외) |
| `points` | array | 빈 배열 (Unity는 트랙 위치만 사용) |
| `events` | array | 이번 프레임에 발생한 이벤트 (없으면 빈 배열) |

#### objects 필드 (v9 변경: 트랙 기반)

| 필드 | 타입 | 설명 |
|------|------|------|
| `id` | int | **트랙 고유 ID** (프레임 간 영속, 이전의 클러스터 인덱스 아님) |
| `x` | float | 트랙 위치 X (mm) |
| `y` | float | 트랙 위치 Y (mm) |
| `type` | string | `"normal"` / `"dumped"` / `"suspect"` / `"abandoned"` |
| `width` | float | 클러스터 폭 (mm) |

### 10.1.1 시각화기 전송 (UDP JSON, 전체 모드)

**대상**: 시각화기 (`127.0.0.1:9090`)
**모드**: 전체 — 모든 클러스터 + 점구름 전송 (디버그용)

시각화기는 기존 형식 유지: `objects`는 클러스터 인덱스 기반, `points`에 4:1 샘플링 점구름 포함.

#### tracks 필드

| 필드 | 타입 | 설명 |
|------|------|------|
| `id` | int | 트랙 고유 ID (생성 시 부여, 영구) |
| `x` | float | 현재 위치 X (mm) |
| `y` | float | 현재 위치 Y (mm) |
| `state` | string | `"moving"` / `"stationary"` / `"departed"` |
| `is_dumped_item` | bool | 투기 확정 물체 여부 |
| `is_dump_suspect` | bool | 투기 의심 물체 여부 |

#### events 필드 (투기 발생 시에만)

| 필드 | 타입 | 설명 |
|------|------|------|
| `type` | string | `"dumping"` |
| `person_track_id` | int | 투기 주체(사람) 트랙 ID |
| `person_x` | float | 사람 위치 X (mm) |
| `person_y` | float | 사람 위치 Y (mm) |
| `object_track_id` | int | 투기물 트랙 ID |
| `object_x` | float | 투기물 위치 X (mm) |
| `object_y` | float | 투기물 위치 Y (mm) |
| `timestamp` | int | 확정 시각 (Unix ms) |

---

### 10.2 FastAPI (DB) 전송 (HTTPS POST, 투기 확정 시에만)

**전송 시점**: 투기 최종 확정 시 1회
**프로토콜**: HTTPS POST
**URL**: `https://api.ecowarden.systems/api/dumping-event`
**인증**: `X-API-Key: <ECOWARDEN_API_KEY>` (키는 `/etc/ecowarden/secrets.conf` 로 주입. v56부터 미설정 시 전송 비활성 — 하드코딩 폴백 제거)
**Content-Type**: `application/json`

```json
{
    "person_id": 1,
    "person_x": 1.2,
    "person_y": -0.8,
    "cumulative_dist": 3.5,
    "object_id": 2,
    "object_x": 0.5,
    "object_y": 0.8,
    "event_time": 1712290800000000000,
    "image_base64": "/9j/4AAQSkZJRg..."
}
```

#### 필드 설명

| 필드 | 타입 | 단위 | 설명 |
|------|------|------|------|
| `person_id` | int | - | 투기 주체(사람) 트랙 ID |
| `person_x` | float | **미터 (m)** | 사람 위치 X (mm → m 변환) |
| `person_y` | float | **미터 (m)** | 사람 위치 Y |
| `cumulative_dist` | float | **미터 (m)** | 사람 누적 이동거리 |
| `object_id` | int | - | 투기물 트랙 ID |
| `object_x` | float | **미터 (m)** | 투기물 위치 X |
| `object_y` | float | **미터 (m)** | 투기물 위치 Y |
| `event_time` | int64 | **나노초 (ns)** | Unix timestamp (밀리초 × 1,000,000) |
| `image_base64` | string | - | 증거 사진 JPEG base64 인코딩 (없으면 필드 생략) |

#### Unity JSON과의 차이점

| 항목 | Unity (UDP) | FastAPI (DB) |
|------|-------------|-------------|
| 좌표 단위 | **mm** (밀리미터) | **m** (미터) |
| 시간 단위 | 밀리초 (ms) | 나노초 (ns) |
| 전송 시점 | 매 프레임 (10Hz) | 투기 확정 시 1회 |
| 사진 포함 | 없음 | base64 JPEG 포함 |
| 전송 실패 시 | 무시 (UDP) | 큐에 저장 후 재전송 |

---

### 10.3 이탈 이벤트 (DepartureEvent, FastAPI 전송)

사람이 정지 후 이탈로 판정될 때 전송됩니다.

```json
{
    "timestamp": "2026-04-15T12:30:00.000Z",
    "x": 1.2,
    "y": -0.35,
    "cluster_id": 5,
    "type": "abandoned"
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `timestamp` | string | ISO 8601 형식 |
| `x` | float | 이탈 위치 X (m) |
| `y` | float | 이탈 위치 Y (m) |
| `cluster_id` | int | 트랙 ID |
| `type` | string | 항상 `"abandoned"` |

---

### 10.4 전송 실패 시 처리

FastAPI 전송이 실패하면:

1. **메모리 큐**에 저장 (pending_queue)
2. **로컬 파일**에 JSONL 형식으로 저장 (`pending_events.jsonl`)
3. **백그라운드 재전송 스레드**가 주기적으로 재시도 (기본 60초 간격)
4. 최대 재시도 횟수: 3회/이벤트
5. 재전송 성공 시 큐에서 제거

UDP(Unity/시각화기)는 실패해도 재전송하지 않습니다 (connectionless).

---

## 11. 판별 결과 요약

| 판별 결과 | 조건 | 시각화 색상 |
|-----------|------|------------|
| **배경** | 정적 배경 필터 또는 배경 물체 필터에서 제거됨 | 표시 안 됨 |
| **사람 (이동)** | 클러스터 매칭 트랙, state=Moving, 누적이동 ≥ 500mm | 빨간색 원 |
| **사람 (정지)** | state=Stationary, 아직 이탈 판정 전 | 빨간색 원 |
| **일반 물체** | 배경 아닌 클러스터, 투기 아닌 것 | 초록색 점 |
| **투기 의심** | is_dump_suspect=true, 확정 대기 중 | 노란색 사각형 |
| **투기 확정** | is_dumped_item=true, 최종 확정 | 노란색 사각형 |

---

## 10. v56 추가 — 분류·프라이버시 단계 (2026-07-31)

§1~§9 의 7단계 판별 흐름 **뒤에** 붙는 단계다. 투기/침입 판정 로직 자체는 무변경.

### 10.1 객체 분류 (`include/object_classifier.h`)

판정된 트랙을 사람/동물/불명으로 가르고 이벤트 등급을 정한다.

| 순서 | 조건 | 결과 |
|---|---|---|
| ① | `age ≤ 3` 이고 `match ≤ 3` | `noise` — **이벤트 전송 안 함** |
| ② | 트래커가 PersonGroup 으로 묶음 | `person` (가장 강한 근거, conf = cap) |
| ③ | 정지 ≥ 10프레임 + 이동 이력 없음 | `static_object` |
| ④ | 폭 < 150mm(사람 최소폭) + 이동 중 | `animal` |
| ⑤ | 속도 > 2200mm/s(보행 상한) + 폭 ≤ 450mm | `animal` |
| ⑥ | 폭 150~900mm + 속도 300~2200mm/s + 매칭 ≥ 5 | `person` |
| ⑦ | 그 외 | `unknown` |

**겹침 대역을 동물로 내리지 않는 이유**
15cm 설치 높이에서 중형견 단면(~250mm)과 사람 다리 하나(~150mm)는 2D LiDAR 로 구분되지 않는다.
초기 구현은 `animal_max_width_mm(450)` 미만이면 동물로 봤는데, 이 대역은 사람 폭 대역 `[150,900]` 과 겹쳐서
④가 도달 불가능해지거나(회귀 테스트가 발견) 진짜 사람을 `low` 로 강등시킬 위험이 있었다.
그래서 동물 판정 근거를 "사람 단면일 수 없다"(④·⑤)로 좁혔다. → 회귀 `OC_object_classifier_unit` Case D3 가 고정.

### 10.2 이벤트 등급 (`GradeEvent()`)

| 분류 | severity | 이벤트 타입 |
|---|---|---|
| `person` conf ≥ 0.5 | `high` | `intrusion` |
| `person` conf < 0.5 | `medium` | `intrusion` |
| `unknown` | **`medium`** | `unknown_object` |
| `animal` / `static_object` | `low` | `animal_or_small_object` |
| `noise` | `ignore` (미전송) | — |

금지구역 안이면 한 단계 상향(`low`→`medium`, `medium`→`high`). `noise` 는 상향하지 않는다.

**원칙**: `unknown` 은 `low` 가 아니라 `medium`. 미탐이 오탐보다 비싸다 — §8.8 의 오탐 방지 철학과 같은 방향이다.

### 10.3 프라이버시 파이프라인 (`camera_module.cpp::PrepareEvidenceFrame`)

사진이 기기 밖으로 나가는 **모든 경로**에서 동일하게 실행된다.

```
프레임 확보
  → ① EvidenceVault::StoreOriginal() 마스킹 前 원본을 AES-256-GCM 암호화 보관
  → ② FaceMasker::Apply() 얼굴 검출 → 블러/모자이크/박스
       ├─ safe_to_emit == false → 사진 저장 자체를 건너뜀 (fail-closed)
       └─ safe_to_emit == true → 계속
  → ③ 배너(ID/ZONE/UTC) + 머리 위 ID 라벨 렌더링
  → 저장·전송
```

파라미터는 `README.md` §3.5 의 `ECOWARDEN_FACE_MASK_*` / `ECOWARDEN_EVIDENCE_*` / `ECOWARDEN_CLASSIFY_*` 표 참조.
